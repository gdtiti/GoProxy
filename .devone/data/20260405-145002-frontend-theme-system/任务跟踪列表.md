# 任务跟踪列表

## 文档元信息

- 任务名称: frontend-theme-system
- 任务目录: .devone/data/20260405-145002-frontend-theme-system
- 创建时间: 20260405-145002
- 工作流档位: 标准全量 (devone)
- 详细设计模式: 经典拆解 (classic)
- 执行模式: 全量 (full)
- Worktree 目录: .devone/worktree/20260405-145002-frontend-theme-system
- Worktree 分支: devone/20260405-145002-frontend-theme-system
- 开发端口: 34248
- 当前阶段: acceptance
- 当前状态: accepted

## 执行策略

- 当前选择:
  - `full`: 全量执行并跟踪必备任务与可选任务
  - `required-only`: 只执行必备任务；可选任务保留记录但默认不推进
- 本次实际模式: full
- 工作流日志: `.devone/wiki/logs/devone-workflow-log.md`（只用于人工检查，不作为平时知识读取入口）
- 结构化回写提示:
  - 更新阶段、波次、聚焦任务时，优先使用 `update-status`
  - 更新单个任务块的类型、状态、前置条件、产出、验证或备注时，优先使用 `update-task-block`
  - 模式切换时，优先使用 `sync-mode`

## 状态枚举

- `pending`: 尚未开始
- `in_progress`: 正在执行
- `blocked`: 有明确阻塞
- `done`: 已完成且已有证据
- `deferred`: 已确认延期
- `skipped_optional`: 因执行模式或用户选择而跳过的可选任务
- `rejected`: 经验收失败

## 任务包状态枚举

- `in_progress`: 当前任务包仍在推进
- `blocked`: 当前任务包存在明确阻塞
- `done`: 实现已完成，待正式验收
- `accepted`: 验收通过
- `conditionally_accepted`: 有条件通过
- `rejected`: 验收未通过

## 必备任务

### R1. 锁定问题定义与执行模式

- 类型: 必备任务
- 状态: done
- 前置条件: 已确认项目根目录
- 产出:
  - `需求说明.md` 的问题定义、目标、范围
  - 记录执行模式
- 验证: 需求说明与任务跟踪列表已同步
- 备注: 需求范围、执行模式与真实问题已锁定；证据来自 .devone/wiki 与 webui 源码。

### R2. 形成可批准设计与阶段计划

- 类型: 必备任务
- 状态: done
- 前置条件:
  - R1 已完成
  - 已收集最小必要上下文
- 产出:
  - `设计书.md`
  - `单元测试设计文档.md` 初稿
- 验证: 存在清晰方案、风险、阶段计划和测试设计
- 备注: 主题系统设计、阶段计划与测试设计已形成，可用于 execution gate 审计。

### R2.5. 创建 worktree 分支与开发端口

- 类型: 必备任务
- 状态: done
- 前置条件:
  - R2 已完成
  - 设计已批准
- 产出:
  - `.devone/worktree/<任务id>/` 目录可用
  - 独立 git 分支已创建/已绑定到 worktree
  - 端口已分配并写回文档元信息
- 验证:
  - `worktree-status` 显示 worktree 已创建且状态清晰
  - `Worktree 目录 / Worktree 分支 / 开发端口` 已写入 `## 文档元信息`
- 备注: worktree=.devone/worktree/20260405-145002-frontend-theme-system；branch=devone/20260405-145002-frontend-theme-system；port=34248

### R3. 完成最小可验证实现

- 类型: 必备任务
- 状态: done
- 前置条件:
  - 设计已批准
  - 关键依赖已确认
- 产出:
  - `webui/theme.go`
  - `webui/html.go`
  - `webui/dashboard.go`
  - `技术说明.md`
- 验证:
  - `rg` 已确认统一主题机制、4 套主题与 `goproxy.theme`
  - `go test ./...` 已通过
  - 页面级验证已证明切换/刷新/i18n/guest-admin 行为正常
- 备注:
  - 真实 GoProxy 二进制受 `CGO_ENABLED=0` + `go-sqlite3` stub 限制，未完成真实 SQLite 后端冷启动；当前页面级验证使用 `.devone-runtime-data/theme_harness.py`。

### R4. 完成验收门禁核对

- 类型: 必备任务
- 状态: done
- 前置条件:
  - R3 已完成
  - 本轮 fresh evidence 已收集
- 产出:
  - 需求映射与门禁结论
- 验证:
  - acceptance audit --target-phase acceptance 已通过
  - 真实二进制 goproxy-theme-nocgo.exe 冷启动成功并生成 proxy.db
  - 真实 :7778 WebUI 页面级主题验证通过
- 备注: 已解除 CGO/SQLite 冷启动阻塞；当前具备进入 completion gate 的证据。

### R4.5. 按主工作区状态回合并并处理 worktree

- 类型: 必备任务
- 状态: blocked
- 前置条件:
  - R4 已完成且验收结论为 `accepted` / `conditionally accepted`
  - 已完成主工作区状态检查，并确认主工作区只执行 git 集成操作
  - worktree `devone/*` 分支已提交完毕并可推送远端
  - 用户明确确认允许执行固定 closeout / 清理动作
- 产出:
  - 已记录本轮固定 closeout 路径：push `devone/*` 分支 -> 从远端主线签出并同步 -> merge -> push 主线
  - 远端 `devone/*` 分支已保留
  - 目标主线已完成 merge+push，或阻塞原因已登记
  - worktree 已按结论清理或保留待后续处理
- 验证:
  - 主工作区 git status --short --branch 显示 main 存在修改 .gitignore 且存在未跟踪 .devone/，当前不满足主工作区干净前提
  - worktree-status 显示 devone/20260405-145002-frontend-theme-system 存在未提交改动与未跟踪 .devone-runtime-data/
  - worktree-merge --into main dry-run 直接报错：Worktree has uncommitted changes; commit everything on the devone branch before closeout
- 备注: 已完成 closeout readiness 检查；当前固定 closeout 被主工作区脏状态与 worktree 未提交改动共同阻塞。下一次应先在 worktree 明确提交范围，再清理主工作区非 git 集成改动后重跑 merge dry-run。

## 可选任务

### O1. 扩展优化与非关键增强

- 类型: 可选任务
- 状态: pending
- 前置条件:
  - 必备任务主链稳定
  - 用户允许扩展范围
- 产出: 增强项或体验优化
- 验证: 不影响必备链路
- 备注:

### O2. 全量回归或更深测试覆盖

- 类型: 可选任务
- 状态: pending
- 前置条件:
  - 核心修复已通过
  - 测试环境可用
- 产出:
  - 更广回归结果
  - 补充测试用例
- 验证: 回归结果已记录到 `技术说明.md` / `单元测试设计文档.md`
- 备注:

## 当前波次

- 当前波次: wave-4
- 当前聚焦任务: R4.5
- 不在本波次的任务: O1, O2

## 变更记录

- [20260405-145002] 初始化任务文档包，执行模式为 `full`
- [20260405-145828] discovery 结论已形成，下一步准备进入 execution gate 与 worktree 创建
- [20260405-145828] R1 完成：问题定义与模式锁定
- [20260405-145828] R2 完成：形成可批准设计与阶段计划
- [20260405-150051] execution gate 已通过，创建独立 worktree 与开发端口
- [20260405-150130] R2.5 已完成，任务包正式进入 execution，下一步实施主题系统
- [20260405-160000] R3 完成：主题系统代码落地，编译链与页面级验证证据已回写
- [20260405-160000] 当前进入 R4：准备执行 acceptance 前 audit
- [20260405-173635] 进入 acceptance；真实二进制冷启动受 CGO/SQLite 环境阻塞，任务包状态标记为 blocked
- [20260405-173648] R4 标记为 blocked：acceptance 已启动，但真实二进制冷启动受 CGO/SQLite 环境阻塞
- [20260405-175924] R4 完成：真实冷启动与真实 WebUI 主题验证补齐，acceptance 门禁已满足
- [20260405-175958] completion gate 通过，任务包状态更新为 accepted；等待用户确认是否进入 R4.5 closeout
- [20260405-180624] R4.5 收尾检查完成：merge readiness 结论为 blocked，原因是主工作区不干净且 worktree 仍有未提交改动
- [20260405-180624] Wave=Closeout；新增；目标=已更新；改动+=3；结果+=2
